# 📋 Diário de Bordo — 2026-04-18
**Projeto:** Delatarot (Flora / Velvet Oráculo)
---

### 15:20 — Link-in-Bio + LP Ebook + Consulta
- Criadas 3 novas páginas: `/links` (link-in-bio), `/consulta` (qualificação WhatsApp), `/ebook` (opt-in ebook)
- Geradas 3 imagens com Gemini (avatar Flora, mockup ebook, hero decorativo)
- Build passando com 0 erros TypeScript
- **Arquivos:** `App.tsx`, `LinksPage.tsx`, `ConsultaPage.tsx`, `EbookPage.tsx`, `EbookHero.tsx`, `EbookOptinForm.tsx`

### 19:07 — Foto de perfil na seção Bio
- Adicionada foto real da Luísa na seção "Quem será sua professora"
- Layout redesenhado com foto + bio lado a lado (desktop) e empilhado (mobile)
- Detalhes decorativos: bordas douradas, anel gradiente, hover zoom, badge com nome
- **Arquivos:** `BioSection.tsx`, `public/images/luisa-perfil.png`

### 19:22 — Imagem de portal na seção final CTA
- Gerada imagem de portal místico dourado com símbolos de tarô
- Adicionada à FinalCTASection com efeito de glow dourado
- **Arquivos:** `FinalCTASection.tsx`, `public/images/portal.png`

### 19:19 — Redesign completo da página do Ebook
- Hero redesenhado: mockup tablet+celular com floating animation e glow dourado
- CTA "Baixar Guia Gratuito" adicionado na primeira seção
- Fonte Inter importada para corpo de texto (melhor leitura)
- Nova seção EbookBioSection apresentando quem é a Luísa
- Design system Velvet Oráculo aplicado (cores, ornamentos, tipografia)
- **Arquivos:** `EbookHero.tsx`, `EbookBioSection.tsx`, `EbookPage.tsx`, `EbookOptinForm.tsx`, `index.css`

### 19:25 — Otimização de contraste em todas as seções
- Overlays escurecidos em todas as seções com fundo escuro (0.45→0.72 / 0.55→0.75)
- Overlays reforçados em seções com fundo claro (0.85→0.93)
- Opacidade de texto aumentada em seções com leitura fraca (/70→/85, /80→/90)
- Criadas classes `.text-shadow-dark` e `.text-shadow-light` no CSS
- **Arquivos:** `index.css`, `HeroSection.tsx`, `PromiseSection.tsx`, `BonusSection.tsx`, `TestimonialsSection.tsx`, `PricingSection.tsx`, `FinalCTASection.tsx`, `IdentificationSection.tsx`, `ModulesSection.tsx`, `BioSection.tsx`, `FAQSection.tsx`, `LinksPage.tsx`, `ConsultaPage.tsx`, `EbookHero.tsx`, `EbookOptinForm.tsx`

### 19:33 — Capa real do ebook + fix layout + responsivo
- Substituída imagem placeholder pela capa real (Capa-ebook.jpeg) nos mockups tablet/celular
- Corrigido bug de sparkle spread (props não-CSS vazando no style)
- Removido card "Taróloga há 7+ anos" da bio para layout mais minimalista
- Layout 100% responsivo com breakpoints sm/md/lg em todas as 3 seções
- **Arquivos:** `EbookHero.tsx`, `EbookBioSection.tsx`, `EbookOptinForm.tsx`

### 19:35 — Remoção de imagem AI + contraste agressivo v2
- Removida imagem AI `portal.png` da FinalCTASection, substituída por textura real `burned-paper.jpg`
- Overlays escurecidos para 0.82 com base `rgba(18,8,8)` em todas as seções escuras
- Opacidades de texto elevadas para /90-100% em todo o site
- Text-shadow reforçado (0.7 opacidade, 20px spread)
- **Arquivos:** `FinalCTASection.tsx`, `HeroSection.tsx`, `PromiseSection.tsx`, `BonusSection.tsx`, `TestimonialsSection.tsx`, `PricingSection.tsx`, `index.css`

### 20:11 — Polish pass completo na página do Ebook
- "100% gratuito · sem spam" movido para baixo do botão (stack vertical)
- Fluxo do hero restaurado: título → desc → benefits → CTA
- Bio: pullquote com borda dourada + texto condensado + foto menor
- Optin: thumbnail do ebook ao lado do formulário (desktop)
- Spacing e opacidades refinados em todas as seções
- **Arquivos:** `EbookHero.tsx`, `EbookBioSection.tsx`, `EbookOptinForm.tsx`

### 20:29 — Formulário ebook expandido + PDF local
- PDF do ebook copiado para `public/` (download direto, sem Google Drive)
- Form expandido: nome, email, WhatsApp (com máscara), experiência com tarô (condicional com nível), expectativas (chips selecionáveis: amor, autoconhecimento, trabalho, dinheiro, espiritualidade, decisões)
- Botão desabilitado até form completo; estado de sucesso com download direto do PDF
- **Arquivos:** `EbookOptinForm.tsx`, `public/taro-do-zero-7-dias.pdf`

### 20:35 — Troca de textura de fundo (cartas)
- Substituída `mystical-table.jpg` por `cartas-fundo.jpeg` (foto real da marca) em PromiseSection e PricingSection
- **Arquivos:** `PromiseSection.tsx`, `PricingSection.tsx`, `public/textures/cartas-fundo.jpeg`

### 20:34 — Foto portrait nas páginas de ebook e vendas
- Formato da foto da Luísa alterado de círculo para portrait retangular editorial
- Moldura dourada com cantos decorativos e hover sutil
- Gradiente inferior com nome/título sobreposto na foto
- Aplicado em ambas as páginas: vendas (BioSection) e ebook (EbookBioSection)
- **Arquivos:** `BioSection.tsx`, `EbookBioSection.tsx`

### 20:53 — Formulário opt-in transformado em modal multi-step
- Formulário convertido de seção para modal/dialog com 3 steps animados
- Step 1: nome, email, WhatsApp (dados de contato)
- Step 2: experiência com tarô + nível condicional
- Step 3: expectativas (chips selecionáveis)
- Cada step traz um argumento de engajamento ("2.000+ mulheres já baixaram")
- Progress bar visual com preenchimento dinâmico
- CTAs no hero e bio abrem o modal; seção de formulário removida da página
- **Arquivos:** `EbookOptinModal.tsx`, `EbookHero.tsx`, `EbookBioSection.tsx`, `EbookPage.tsx`

### 20:57 — Checkout pré-populado Kiwify + pricing parcelado
- CTAButton refatorado: abre modal de captura (nome, email, WhatsApp) antes de redirecionar
- Dados salvos como lead no localStorage e enviados como query params ao checkout Kiwify
- PricingSection: valor parcelado (12x R$35,68) agora é o destaque principal, à vista é secundário
- Modal com animações de entrada (backdrop fade + card slide-up)
- **Arquivos:** `CTAButton.tsx`, `CheckoutModal.tsx`, `PricingSection.tsx`, `FinalCTASection.tsx`, `index.css`

### 21:49 — UX validação inline + fix flicker modal ebook
- Botão "Continuar" sempre ativo: ao clicar com campos faltando, indica erro inline com foco automático no campo
- Erros somem automaticamente ao corrigir cada campo
- Shake animation no botão quando validação falha
- Fix de flicker: StepWrapper removido do render, inputs com transition-colors, nível com max-height CSS
- ARIA: dialog, progressbar, fieldset/legend, aria-pressed, radiogroup
- **Arquivos:** `EbookOptinModal.tsx`, `index.css`

### 21:58 — Fix modal checkout cortado
- Modal refatorado: backdrop agora é `overflow-y-auto` com wrapper `min-h-full flex items-center`
- Corrige o modal sendo cortado em viewports pequenas (WhatsApp + botão ficavam escondidos)
- Click-outside melhorado com `data-modal-card` para fechar ao clicar fora do card
- **Arquivos:** `CheckoutModal.tsx`

### 22:08 — Rewrite limpo do EbookOptinModal
- JSX estava quebrado por edits incrementais (fieldErrors órfãos, nível error no div errado)
- Reescrito do zero: FieldError component, inputNormal/inputError classes, clearError centralizado
- Modal scrollável em telas pequenas (overflow-y-auto + min-h-full)
- Validação inline com glow pulsante rosa + shake no botão + foco automático
- **Arquivos:** `EbookOptinModal.tsx`, `index.css`

### 22:14 — Fix modal checkout renderizando inline
- Causa raiz: `position: fixed` quebra quando pai tem `transform` (via `animate-fade-in-up`)
- Fix: `createPortal(modal, document.body)` renderiza modal fora da árvore DOM das seções
- **Arquivos:** `CheckoutModal.tsx`

### 22:30 — Push completo para GitHub
- Commit `dfb4b70` com 35 arquivos (2856+, 93-) enviado para `visualizen/velvet-oraculo`
- `.gitignore` atualizado para excluir assets de referência da raiz
- **Arquivos:** `.gitignore`
