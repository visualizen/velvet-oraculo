# 📋 Diário de Bordo — 2026-04-22
**Projeto:** Velvet Oráculo
---

### 20:48 — Fix bug formulário ebook (duplicatas)
- Diagnosticado via logs Supabase: todos os erros eram `409 duplicate key` (email já cadastrado)
- Causa raiz: `insert` falhava + error handling usava `instanceof Error` (não detecta PostgrestError do Supabase)
- Fix: trocado `insert` por `upsert` com `onConflict: "email"` + detecção genérica de erro
- **Arquivos:** `src/components/EbookOptinModal.tsx`

### 20:53 — Auditoria tipografia desktop + remoção StickyNav
- Bump global `html font-size` 16px→18px em `lg` e 19px em `2xl` — escala tudo automaticamente via rem
- Removido `StickyNav` da página de vendas (redundante com FloatingCTA)
- TypeScript 0 erros, build OK, push para produção
- **Arquivos:** `src/index.css`, `src/pages/Index.tsx`
