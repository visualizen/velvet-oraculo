# 📋 Diário de Bordo — 2026-04-22
**Projeto:** Velvet Oráculo
---

### 20:48 — Fix bug formulário ebook (duplicatas)
- Diagnosticado via logs Supabase: todos os erros eram `409 duplicate key` (email já cadastrado)
- Causa raiz: `insert` falhava + error handling usava `instanceof Error` (não detecta PostgrestError do Supabase)
- Fix: trocado `insert` por `upsert` com `onConflict: "email"` + detecção genérica de erro
- **Arquivos:** `src/components/EbookOptinModal.tsx`

### 20:53 — Auditoria tipografia desktop + remoção StickyNav
- Bump global `html font-size` 16px→18px em `lg` e 19px em `2xl` — escala tudo automaticamente via rem
- Removido `StickyNav` da página de vendas (redundante com FloatingCTA)
- TypeScript 0 erros, build OK, push para produção
- **Arquivos:** `src/index.css`, `src/pages/Index.tsx`

### 21:22 — Fix RLS policy + deploy ebook form
- Causa raiz: tabela `ebook_leads` tinha policy INSERT mas **não UPDATE** — upsert falhava silenciosamente
- Adicionada migration `add_update_policy_ebook_leads` no Supabase
- Error handling melhorado: emails duplicados agora tratados como sucesso (redirect para download)
- Deploy produção Vercel: velvetoraculo.com.br
- **Arquivos:** `EbookOptinModal.tsx`, Supabase RLS migration

### 22:57 — Fix definitivo: RLS SELECT policy + image fallback
- **Causa raiz real do form:** PostgREST retornava `401` (não 409) porque `upsert` com `on_conflict` exige policy SELECT (para verificar conflitos) — faltava essa policy
- Adicionada migration `add_select_policy_ebook_leads` — testada via curl: 201/200 ✓
- **Imagens quebradas:** navegadores in-app (TikTok BytedanceWebview) bloqueiam carregamento de imagens
- Criado componente `ImgWithFallback` com `crossOrigin`, `referrerPolicy` e fallback emoji via `onError`
- Deploy produção Vercel: velvetoraculo.com.br
- **Arquivos:** `EbookOptinModal.tsx`, Supabase migration `add_select_policy_ebook_leads`

### 23:05 — Fix definitivo: imagens + PDF + título ebook
- **Causa raiz imagens e PDF:** `.vercelignore` tinha `*.pdf` e `Capa-ebook.jpeg` (wildcards) que bloqueavam assets legítimos em `public/`
- Reescrito `.vercelignore` com paths explícitos `/root-file.ext` em vez de wildcards
- Validado via curl: imagem retorna `image/jpeg`, PDF retorna `application/pdf` ✓
- Título EbookHero aumentado 50% (1.75rem→2.625rem mobile, md:5xl→7xl)
- Texto separado em 2 parágrafos com melhor hierarquia + benefits font bump
- **Arquivos:** `.vercelignore`, `EbookHero.tsx`, `EbookOptinModal.tsx`
