# 📋 Diário de Bordo — 2026-04-19
**Projeto:** Velvet Oráculo
---

### 00:06 — Ajuste de copy: remoção de claim inventado
- Substituída frase "Mais de 2.000 mulheres já baixaram..." por hook baseado em benefício real, sem inventar métricas
- **Arquivos:** `src/components/EbookOptinModal.tsx`

### 00:09 — Ajuste tamanho "12x" no preço + deploy
- Aumentado texto "12x de" de text-sm para text-xl na seção de preços
- Push para produção (main)
- **Arquivos:** `src/components/sections/PricingSection.tsx`

### 14:49 — Fix redirect WhatsApp no browser TikTok
- `window.open("_blank")` → `window.location.href` na ConsultaPage (mesmo fix do checkout)
- **Arquivos:** `src/pages/ConsultaPage.tsx`

### 14:54 — Fix vídeo Vimeo não carregando
- Removido parâmetro `dnt=1` que ativava Cloudflare Turnstile e bloqueava o player
- Adicionado `web-share` ao atributo `allow` do iframe (padrão oficial Vimeo)
- **Arquivos:** `HeroSection.tsx`, `VideoSection.tsx`

### 15:02 — Fallback vídeo para in-app browsers
- Detecta TikTok/Instagram/Facebook webview e mostra thumbnail+play ao invés de iframe
- Timeout de 8s: se vídeo não carrega, mostra fallback com link direto pro Vimeo
- **Arquivos:** `HeroSection.tsx`

### 15:06 — Correção definitiva do Autoplay no Vimeo
- Adicionados os parâmetros perfeitos recomendados para autoplay in-app: `background=1`, `autoplay=1`, `loop=1`, `muted=1`, `playsinline=1`.
- Adicionados os atributos no iframe: `playsInline`, `allowFullScreen`, `webkit-playsinline="true"`.
- Removido o botão do Youtube / Link pro vimeo. O iframe renderiza perfeitamente em background.
- **Arquivos:** `HeroSection.tsx`, `VideoSection.tsx`

### 15:37 — Fallback <video> nativo para in-app browsers
- Converteu vídeo MOV (133MB) → MP4 H.264 (24MB) com ffmpeg
- Upload do MP4 para Supabase Storage (bucket `videos`, público)
- Detecta TikTok/IG/FB in-app → usa `<video>` nativo com Supabase CDN
- Browser normal → mantém Vimeo iframe com controles customizados
- **Arquivos:** `HeroSection.tsx`, `.gitignore`

### 18:54 — Refatoração completa do video player
- Removido Vimeo completamente — agora usa APENAS `<video>` nativo com Supabase CDN
- Fix fullscreen: play via JS (não via atributo autoPlay), playsinline forçado antes do play()
- UI minimalista: barra de progresso fina + botão circular de unmute
- Progress bar acelerada: primeiros 50% do vídeo preenchem 75% da barra (sensação de vídeo curto)
- **Arquivos:** `HeroSection.tsx`
